current_hours = 14;
current_minutes = 34;
current_seconds = 42;

# Write a program that prints the remaining seconds (as an integer) from a
# day if the current time is ree
#presented bt the variablesr
remaining_hours = 23 - current_hours
remaining_minutes = 59 - current_minutes
remaining_seconds = 60 - current_seconds

print(str(remaining_hours) + ":" + str(remaining_minutes) + ":" + str(remaining_seconds))